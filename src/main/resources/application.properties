spring.application.name=fotos

# Perfil activo por defecto (dev = local, prod = S3)
spring.profiles.active=${SPRING_PROFILES_ACTIVE:prod}

spring.datasource.url=jdbc:postgresql://localhost:5432/campamento-sanpablo
spring.datasource.username=enriquefernandez
spring.datasource.password=Hi-alsoWm24
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Configuración de subida de archivos
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Almacenamiento local (perfil dev)
app.upload.dir=./uploads
app.base-url=http://localhost:8080

# AWS S3 (perfil prod)
aws.s3.bucket=${S3_BUCKET_NAME:campamento-san-pablo-fotos}
aws.region=${AWS_REGION:sa-east-1}
aws.cloudfront.url=${AWS_CLOUDFRONT_URL:https://d3bu2wb9bvmk3d.cloudfront.net}

# JWT Configuration
jwt.secret-key=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration=86400000
